# v0.5.8 发布收尾 — 修改记录 / Release Closeout Changelog

> 撰写日期：2026-05-11
> 对应提交：`077deb1`（合并到 main），`f3c3fd4`（补充文档）

---

## 本次变更 / Changes

### 1. PR 冲突解决与合并
- **冲突文件**：6 个文件存在合并冲突
  - `CHANGELOG.md` — 合并两个 v0.5.8 条目（SerpAPI 多 Key + 搜索源/AI 精简）
  - `src/routes/idea.rs` — 5 处 `PipelineRunner::new()` / `Orchestrator::new()` 签名变更（移除 bing/lens 参数）
  - `src/routes/mod.rs` — `AppConfig` 结构体字段精简（移除 bing/lens/firecrawl/cnipr）
  - `src/routes/search.rs` — 移除 Firecrawl/Bing/CNIPR 搜索代码
  - `src/routes/settings.rs` — 移除已废弃的搜索/AI 设置接口
  - `templates/settings.html` — 移除"高级设置"区域（备用搜索/AI/CNIPR 配置）
- **合并策略**：对每个冲突取 worktree 分支版本（移除废弃代码）
- **合并结果**：`077deb1 Merge branch 'claude/lucid-engelbart-8f807d' into main`

### 2. 验证全通过
| 检查项 | 结果 |
|--------|------|
| `cargo fmt --check` | ✅ 通过 |
| `cargo clippy -- -D warnings` | ✅ 零警告 |
| `cargo test` | ✅ 109 项全通过 |
| `cargo build --release` | ✅ 构建成功（15.9MB） |

### 3. 标签与推送
- `v0.5.8` 标签从 worktree 分支移动到 main 的 merge commit
- 推送到 GitHub + Gitee（main 分支 + 标签）

### 4. Release 页面更新
- 说明文字更新为完整 v0.5.8 变更内容
- Windows 安装包重新编译上传（6.1MB zip）
- 链接：https://github.com/jsshwqz/innoforge/releases/tag/v0.5.8

### 5. 清理
- 删除临时文件 `innoforge-windows-x86_64.zip`
- 清除 2 个旧 git stash（Firecrawl 废弃代码）

### 6. 文档
- 更新 `CHANGELOG.md`（合并两个 v0.5.8 条目）
- 新增 `docs/records/` 三份文档（changelog / retrospective / errors）
- 新增 `docs/plans/2026-05-11-v0.5.9-plan.md`

---

## 未完成 / Not Completed

| 事项 | 说明 |
|------|------|
| 远程 worktree 分支清理 | `origin/claude/lucid-engelbart-8f807d` 已合并到 main，可删除 |
| 本地 worktree 目录清理 | `.claude/worktrees/lucid-engelbart-8f807d/` 仍存在 |
| Gitee Release 说明同步 | 仅更新了 GitHub，Gitee 未同步 |
| Linux/macOS 安装包 | 需要 CI 或对应编译环境 |
