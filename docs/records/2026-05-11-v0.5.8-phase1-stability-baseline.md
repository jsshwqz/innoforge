# v0.5.8 阶段1稳定收尾基线记录

## Task Goal
- 执行发布后稳定收尾第1阶段：
  1. `fmt + clippy + test + release build` 全量基线验证  
  2. `v0.5.8` 发布资产完整性抽检  
  3. 仓库双端一致性核对（origin / gitee）

## Forge Calls
1. Tool: `mcp__aion-forge__ai_research`  
   Input: 发布后稳定收尾时的风险点与检查顺序  
   Output Summary: 返回研究流程结果，可用于本轮检查顺序与风险控制参考。

## Execution Decision
- Forge 产出：方案级检查顺序参考。  
- 本地执行：实际命令验证、资产抽检、双端一致性核验。  
- 取舍原因：forge 当前不直接执行本地构建命令，因此由本地落实并落档。

## Verification
- `cargo fmt --all`：通过  
- `cargo clippy --tests -- -D warnings`：通过  
- `cargo test --tests`：失败（34 通过 / 1 失败）  
  - 失败用例：`experiment::sandbox::tests::test_simple_python_experiment`  
  - 结论：为现有基线失败，非本轮新增改动引入  
- `cargo build --release`：通过  
  - 备注：存在已知 warning（`innoforge.pdb` 输出文件名冲突）

- `gh release view v0.5.8 --json assets,url`：5 个资产齐全，且均有下载计数（`downloadCount=1`）  
  - Linux x86_64 / aarch64  
  - macOS x86_64 / aarch64  
  - Windows x86_64

- 双端一致性核验：  
  - `HEAD` = `origin/main` = `gitee/main` = `609cf19737b0e0cd48d03afcb90dcf46b6ccc032`

## 结论
- 阶段1稳定收尾已完成。  
- 当前唯一未过项为既有测试基线失败（sandbox 单测 1 项），建议在阶段2并行处理该用例稳定性，避免影响后续全绿门禁。
